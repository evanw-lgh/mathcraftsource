# Mathcraft texture pack

Add your own PNG files here using the names in `textures.py`.
The game automatically uses `missing.png` when an image is not available.

The texture manifest is intentionally kept in Python so block art can be
swapped without changing world-generation code.
